spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://Keycloak:8080/realms/ms-curso-fila-realms  # http://${KEYCLOAK_SERVER}:${KEYCLOAK_PORT}/realms/ms-curso-fila-realms #"http://Keycloak:8080/realms/ms-curso-fila-realms"

server:
  port: 8080

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://projeto-fila-eurika:8761/eureka #http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  file:
    name: ./logs/log-file.log